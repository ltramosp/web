"""
GIT: Es un sistema de control de versiones distribuido que nos permite rastrear cambio en el codigo fuente durante el desarrollo.
- me ayuda a tener un historial detallado de los cambios
- facilita la colaboracion entre equipo.

CARACTERISTICAS:
-rastreo de cambios.
-distribuido.
-ramas:

GITHUB:plataforma basada en la web que utiliza git. proporciona un entorno para que los proyectos  sean alojados en la nube.

CARACTERISTICAS:
-clonar un repositorio
-hacer cambios
-guardar cambios (commit)
-subir los cambios (push)
-colaborativa.

------------------------------------------------------------------------------------
git init : inicializa un nuevo repositorio git en una carpeta
git clone: crea una copia del repositorio remoto
git status: me muestra el estado actual del repositorio, incluido los archivos que se han modificado
git add:  añade archivos al area de preparacion (git)
git commit: guarda los cambios en el repositorio con un mensaje
git push : envia los cambios confirmado(commit) desde el repositorio local al github
git pull: descarga y combina los cambios del repositorio reomoto en el repositorio local
git brach: permite trabajar con diferentes ramas
git checkout: se cambia en una rama especifica, restaurar versiones
git log: muestra el hostorial de los commit del repositorio
git remote: administra los repositorios remotos
"""